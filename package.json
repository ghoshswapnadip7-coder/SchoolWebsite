{
  "name": "school",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install-all": "npm install && npm install --prefix backend && npm install --prefix school",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix school\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
